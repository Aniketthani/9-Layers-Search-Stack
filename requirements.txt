# Document processing
pdfplumber==0.11.0
python-docx==1.1.2
openpyxl==3.1.2
extract-msg==0.48.0
beautifulsoup4==4.12.3
lxml==5.2.2

# NLP + Matching
pyahocorasick==2.1.0
nltk==3.8.1

# sentence-transformers — fallback embedder when Azure OpenAI is not configured
# Pinned to avoid pulling torch which massively increases cold-start time on Cloud
sentence-transformers==3.0.1
torch==2.3.1+cpu --extra-index-url https://download.pytorch.org/whl/cpu

# Vector DB — telemetry disabled in code; pinned to avoid breaking changes
chromadb==0.5.3

# LLM
openai>=1.40.0
groq==0.9.0

# UI
streamlit==1.36.0
streamlit-extras==0.4.3

# Utilities
python-dotenv==1.0.1
pandas==2.2.2
pydantic==2.7.4
loguru==0.7.2
httpx>=0.27.0
